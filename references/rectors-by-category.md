# Rectors by Category

Category → rules mapping. Small–medium; meant to be read whole as the **first**
lookup step in the rule-selection priority (see `SKILL.md`).

> Skeleton placeholder — to be populated. See `CONTEXT.md` → "Reference documents".
